%div
  Filtrar por:
  %br
  Ano
  = text_field_tag :ano, nil, type: 'number', rel: 'filter'
  %br
  Titulo
  = text_field_tag :titulo, nil, rel: 'filter'
  %br
  Tipo
  = select_tag :tipo, options_for_select([['',''], ['Periódico', 'Periodico'], ['Conferência', 'Conferência'], ['Livro', 'Livro']]), rel: 'filter'
  %br
  Ordernar por:
  = select_tag :order, options_for_select([['',''], ['Ano', 'ano'], ['Título', 'titulo'], ['Tipo', 'tipo']]), rel: 'order'

%br

%div
  = link_to "Incluir publicação", new_pub_path
  = link_to "Administrar usuaŕios", user_index_path
  = link_to "Gerar embed code", '#'

%hr
%ul.pub-list
  - @pubs.each do |pub|
    %li.pub-record
      %span.pub-tipo [#{pub._type}] 
      %span.pub-ano [#{pub.ano}]
      %span.pub-titulo= link_to pub.titulo, pub_path(:id => pub.id)
      %br
      #{pub.autores.join(', ')}